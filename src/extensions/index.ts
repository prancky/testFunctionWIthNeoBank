/**
 * Custom function registry (user-owned — preserved across regenerations).
 *
 * Register functions here so they can be called from widget events (onPress, onChange,
 * ...) configured in the designer with a CUSTOM_FUNCTION handler. The designer's
 * "function name" must match an exported name below. Each function receives a context
 * `{ value, dataMapping }` (value = the event payload, e.g. an onChange value).
 *
 * Example:
 *   export async function trackEvent({ dataMapping }: { value?: any; dataMapping?: any }) {
 *     console.log('track', dataMapping);
 *   }
 */

export {};
