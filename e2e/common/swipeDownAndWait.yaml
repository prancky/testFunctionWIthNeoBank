appId: "${APP_ID}"
name: "Swipe Down and Wait"
# Common reusable flow: Swipe element down and wait for animation
---
- swipe:
    from:
      id: "${WIDGET_ID}"
    direction: DOWN
- waitForAnimationToEnd: {}
