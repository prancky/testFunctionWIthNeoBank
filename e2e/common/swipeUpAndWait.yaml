appId: "${APP_ID}"
name: "Swipe Up and Wait"
# Common reusable flow: Swipe element up and wait for animation
---
- swipe:
    from:
      id: "${WIDGET_ID}"
    direction: UP
- waitForAnimationToEnd: {}
